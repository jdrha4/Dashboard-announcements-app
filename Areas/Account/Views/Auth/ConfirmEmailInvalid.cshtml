@{
    ViewData["Title"] = "Invalid or expired token";
}

<div class="flex-grow-1 d-flex align-items-center my-3">
    <div class="container">
        <div class="row justify-content-center">
            <div class="bg-container col-11 col-sm-7 col-md-6 col-lg-5 col-xl-4 p-4 shadow rounded-4">

                <!-- Title -->
                <h1 class="text-center mb-1 fw-bold fs-3">INVALID OR EXPIRED TOKEN</h1>
                <p class="text-center mb-3 text-dark fs-5">
                    For your security, confirmation links are time-limited on announce<span class="fw-bold text-orange">IT</span>
                </p>

                <!-- Back to home button -->
                <a asp-route="default" class="btn btn-orange w-100 py-2 mt-3 rounded-3">
                    <i class="bi bi-house-door me-1"></i>back to home
                </a>
            </div>
        </div>
    </div>
</div>
